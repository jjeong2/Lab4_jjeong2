<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_spl"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorBack"
    tools:context="com.cs60333.username.lab2_app.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/main_text"
        android:textSize="@dimen/main_textsize"
        android:textColor="@color/colorwhite"
        tools:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:id="@+id/main_text"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/support"
        android:textColor="@color/coloryellow"
        android:textSize="@dimen/support_textsize"
        tools:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:id="@+id/support_text"
        android:layout_below="@id/main_text"
        android:layout_margin="@dimen/activity_horizontal_margin"/>

    <ImageView
        android:layout_height="@dimen/main_image"
        android:layout_width="@dimen/main_image"
        android:src="@drawable/ndlog"
        android:layout_centerHorizontal="true"
        android:id="@+id/main_image"
        android:layout_below="@id/support_text" />


</RelativeLayout>
